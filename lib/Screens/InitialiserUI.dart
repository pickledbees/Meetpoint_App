import 'package:flutter/material.dart';
import 'HomeView.dart';

class InitialiserUI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    //if first start

    return Scaffold(
      body: Center(
        child: Text('Meetpoint'),
      ),
    );
  }
}